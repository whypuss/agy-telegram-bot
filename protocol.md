# 繁體中文高效代理協定 (Token-Efficient Agent Protocol)

## 輸出規範 (Output)
- 嚴禁打印大型終端輸出。
- 預期超過 50 行的指令必須重定向到檔案，使用 grep、head、tail 進行定向閱讀。
- 除非使用者明確要求，否則不打印完整 git diff。

## 目錄與搜尋規範 (Directory & Search Scope)
- 嚴禁從家目錄頂層執行無差別全域深度搜尋（嚴禁 `find /Users/whypuss` 或 `find ~`），避免遍歷觸碰系統受保護目錄（如 Library、相簿、日曆等）而觸發 macOS 系統隱私彈窗。
- 搜尋專案或代碼時，必須優先精確鎖定在 `~/projects/` 或目標專案子目錄內搜尋。

## 狀態維護 (State)
- 維護 `~/.antigravity/task_state.md`，記錄核心發現、決策、阻礙與下一步。
- 不重複記錄已知資訊，專注實質進展。

## 程式碼編輯 (Editing)
- 堅持精確修剪（Surgical Edits），能用局部補丁解決的絕不全檔重寫。

## 驗證原則 (Verification)
- 優先執行最小關聯測試。
- 測試失敗時，重定向至日誌並僅檢查相關片段，不重複跑未變更的耗時測試。
- 嚴禁因指令返回碼 0 或編輯成功就假設修改有效，必須驗證受影響的行為。

## Git 規範 (Git)
- 變更前檢查 `git status --short`。
- 詳細 diff 前優先查看 `git diff --stat`。
- 保持提交語義連貫。

## 思考與排版規範 (Thinking & Formatting Style)
- **思維語言（Thinking Language）**：
  思考過程（Thinking / Chain of Thought）必須**全程使用繁體中文**進行深度推理、用戶需求分析與步驟規劃，**嚴禁使用英文思考**！
- **實質思考（Real Reasoning）**：
  思考內容必須是實質的技術取捨、架構分析與方案推導，嚴禁輸出機械式模板套話、空代碼塊或重複前文。
- **直出不折疊（Unfolded Display）**：
  絕不使用可折疊引用塊（**>）或 HTML `<details>` 標籤，所有思考與推導必須以標準段落直出呈現，確保在 Telegram 中一目了然。

## 執行閉環與禁止承諾停滯規範 (Autonomous Completion Invariant)
- **嚴禁未來時態承諾 (No Future-Tense Promises)**：
  嚴禁在回覆中宣稱「我稍後會執行...」、「接下來我將在背景完成...」、「請稍候，等會會自動處理...」然後停止呼叫工具。
  在 Telegram 終端中，每一次使用者發問都是一次獨立的自主執行輪次。一旦停止呼叫工具，CLI 程序即宣告退出，**根本沒有任何後台進程會自動替你接續執行**！
- **必須在當前回合連續調用工具執行到底 (Must Complete in Current Turn)**：
  如果任務包含多個步驟（例如：修改程式碼 -> 執行測試 -> 修復報錯 -> 重新驗證 -> Git 提交/推送），你必須**在當前輪次中連續調用工具**，一步步執行到底，直到所有承諾的事項「100% 落地且驗證通過」為止。
- **嚴禁將長任務遺棄在背景 (No Orphaned Background Tasks)**：
  若執行耗時命令（如測試、構建、依賴安裝）轉入背景任務，必須持續跟進檢查狀態（`manage_task` 或日誌輪詢），直到確認完成並驗證輸出，絕對禁止在留下一句「正在背景執行」後便停止調用工具。
- **自主推進禁止隨意中斷**：
  只要目標明確且無阻塞性歧義，不可無故停下要求使用者確認「是否要繼續」，必須直接自主推進至終點。

## 服務生命週期維護安全規範 (Service Lifecycle Safety)
- **嚴禁在對話中途立即自我重啟**：
  當任務涉及修改 Telegram Bot 自身的程式碼（如 `bot.py`、`agent_runner.py`）時，**嚴禁直接調用 `restart.sh`、`launchctl kickstart` 或 `pkill -f bot.py`**！
  此舉會立刻引發系統發送 SIGTERM 終止當前運行的主進程，導致當前正在對話的子進程被強行斬斷，引發對話死鎖與用戶端無響應。
  修改完成後，應完成驗證並在回覆中總結完畢，交由用戶決定重啟或調用安全的異步延遲重啟。
